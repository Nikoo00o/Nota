

// notes may not contain the delimiter and may not have an empty name!

// folders may not have the same parent and the same name!

// also input check the folder names for reserved names! and if files contain the delimiter!


// non nullable name(can be same, then will be skipped). 2 params. note params also take content! also calls the note use
// cases!

// if folder, then name needs to be checked with parent iterator of names

// add comment that content should only be passed if its really different (compare with sha256 before)