


// important: special workflow for new note file. because it has no id yet in the stored note repository, so it cant be
// refreshed. must be manually set from here!!! shows dialog where name has to be entered (same as with folder) and that
// creates a new id

// dialog service needs input dialog for that